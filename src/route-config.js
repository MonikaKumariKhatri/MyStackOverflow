import React from "react";
import { HashRouter } from "react-router-dom";
import App from "./App";

export const RouteConfig = () => {
  return (
    <HashRouter>
      <App />
    </HashRouter>
  );
};
